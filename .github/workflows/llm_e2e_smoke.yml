name: LLM E2E Smoke

on:
  workflow_dispatch:
  push:
    branches: [develop]
    paths:
      - '.github/workflows/llm_e2e_smoke.yml'
      - '.github/workflows/android_mvp_ci.yml'
      - '.github/workflows/android_emulator_smoke.yml'
      - 'scripts/llm_e2e_emulator.sh'
      - 'lib/main.dart'
      - 'lib/features/model/recommended_model_installer.dart'
      - 'android/app/build.gradle.kts'
      - 'android/app/src/debug/**'
      - 'android/app/src/main/kotlin/com/householder/app/RecommendedModelPackDownloader.kt'
      - 'android/app/src/main/kotlin/com/householder/app/LocalLlamaEngine.kt'

permissions:
  contents: read

jobs:
  llm-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install Flutter dependencies
        run: flutter pub get

      - name: Free runner disk space
        run: |
          sudo rm -rf /usr/share/dotnet || true
          sudo rm -rf /opt/ghc || true
          sudo rm -rf /usr/local/share/boost || true
          df -h

      - name: Build the same debug APK used by the app
        run: flutter build apk --debug

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Download model and run one Llama inference
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          target: google_apis
          arch: x86_64
          profile: pixel_5
          cores: 2
          ram-size: 6144M
          heap-size: 1024M
          disk-size: 8G
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none -camera-front none
          disable-animations: true
          script: bash scripts/llm_e2e_emulator.sh

      - name: Upload LLM E2E evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: householder-llm-e2e
          path: llm-e2e-artifacts/
          if-no-files-found: warn
          retention-days: 7
